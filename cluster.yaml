apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: go-web-app
  region: ap-south-1
  version: "1.30"

managedNodeGroups:
  - name: go-web-app-free
    instanceType: t2.micro
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
    volumeSize: 20
